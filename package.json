{
  "name": "your-s-tools",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --filter ./packages/** run dev",
    "build": "pnpm -r run build",
    "build:watch": "pnpm -r --parallel run build:watch",
    "build:watch:turbo": "turbo run build:watch",
    "catalog": "pnpx codemod pnpm/catalog",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": "^20.19.3",
    "pnpm": "^10.10.0"
  },
  "dependencies": {
    "@types/node": "catalog:",
    "consola": "catalog:",
    "vue-starport": "catalog:"
  },
  "devDependencies": {
    "@types/chrome": "catalog:",
    "turbo": "^2.8.7",
    "vite-plugin-static-copy": "catalog:"
  }
}
